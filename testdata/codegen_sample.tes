funk <int> add(a as int, b as int)
{
    result :: int = a + b;
    return result;
}

funk <null> main()
{
    a :: int;
    b :: int;

    a = scan();
    b = scan();

    print(add(a, b));

    return 0;
}
